audiencias <-
read.table(file = "./Data/audiencias.csv",      # Ficheiro da fonte de dados
header = FALSE,          # Primeira linha tem dados
sep = ",",               # Valores separados por vírgulas
dec = ".",               # Separador decimal é um ponto
comment.char = "");      # Cada linha só tem dados
help("write.table")
classes <- write.table(dados,
file = "./Data/classes.tsv",           # Formato adequado ao Excel
sep = "\t",                   # Campos separados por tabulações
row.names = FALSE,            # Não mostrar números de linhas
col.names = TRUE);            # Nomes dos campos na primeira linha
classes <- data.frame();
classes <- data.frame();
write.table(classes,
file = "./Data/classes.tsv",           # Formato adequado ao Excel
sep = "\t",                   # Campos separados por tabulações
row.names = FALSE,            # Não mostrar números de linhas
col.names = TRUE);
classes <- data.frame();
write.table(classes,
file = "./Data/classes.tsv",           # Formato adequado ao Excel
sep = "\t",                   # Campos separados por tabulações
row.names = FALSE,            # Não mostrar números de linhas
col.names = TRUE);
View(classes)
View(classes)
espetadores <-
read.table(file = "./Data/espetadores.csv",
header = TRUE,
sep = "\t")
classes <-
read.table(file = "./Data/classes.tsv",
header = TRUE,
sep = "\t")
rm
rm(list=ls(all=TRUE))
audiencias <-
read.table(file = "./Data/audiencias.csv",      # Ficheiro da fonte de dados
header = FALSE,          # Primeira linha tem dados
sep = ",",               # Valores separados por vírgulas
dec = ".",               # Separador decimal é um ponto
comment.char = "");      # Cada linha só tem dados
espetadores <-
read.table(file = "./Data/espetadores.csv",      # Ficheiro da fonte de dados
header = FALSE,          # Primeira linha tem dados
sep = ",",               # Valores separados por vírgulas
dec = ".",               # Separador decimal é um ponto
comment.char = "");      # Cada linha só tem dados
classes <-
read.table(file = "./Data/classes.tsv",
header = TRUE,
sep = "\t");
tipologia <-
read.table(file = "./Data/tipologia.tsv",
header = TRUE,
sep = "\t")
summary(audiencias)
